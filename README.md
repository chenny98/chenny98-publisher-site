# CHENNY98 Publisher Site

Static publisher profile prepared for GitHub Pages.

## Publish

1. Create a public GitHub repository.
2. Upload `index.html` and the `assets` folder to the repository root.
3. In repository Settings → Pages, deploy from the `main` branch and `/ (root)`.

